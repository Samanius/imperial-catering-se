# Imperial Catering - Система Централизованной Базы Данных

## ⚠️ ВАЖНО ДЛЯ VERCEL DEPLOYMENT

Это приложение теперь использует **централизованную облачную базу данных** через GitHub Gist API.

### 🔴 БЕЗ НАСТРОЙКИ БАЗЫ ДАННЫХ:
- ❌ Рестораны не будут сохраняться
- ❌ Данные будут теряться при обновлении страницы
- ❌ Каждый пользователь будет видеть пустой сайт

### ✅ С НАСТРОЕННОЙ БАЗОЙ ДАННЫХ:
- ✓ Все данные хранятся в облаке (GitHub Gist)
- ✓ Все пользователи видят одинаковые рестораны
- ✓ Данные не теряются при деплое
- ✓ Автоматическое резервное копирование

## 🚀 Быстрый Старт

### 1. Создайте GitHub Token

```
1. Перейдите: https://github.com/settings/tokens/new
2. Название: Imperial Catering Database
3. Scope: ✓ gist (только это!)
4. Срок: No expiration
5. Generate token
6. СОХРАНИТЕ токен (начинается с ghp_...)
```

### 2. Настройте Базу Данных

После деплоя на Vercel:

1. Откройте ваш сайт
2. Нажмите на кнопку Admin в шапке
3. Перейдите на вкладку **"Database"**
4. Выберите **"Create New"**
5. Вставьте GitHub token
6. Нажмите **"Create Database"**
7. **СОХРАНИТЕ Gist ID** который система покажет!

### 3. Добавьте Рестораны

Теперь вы можете:
- Создавать рестораны через админ-панель
- Импортировать из Google Sheets
- Все изменения автоматически сохраняются в облаке

## 📖 Полная Документация

См. [DATABASE_SETUP_RU.md](./DATABASE_SETUP_RU.md) для подробных инструкций.

## 🔧 Как Это Работает

```
┌─────────────┐
│   Browser   │  ← Пользователь видит сайт
└──────┬──────┘
       │
       ↓
┌─────────────┐
│  React App  │  ← Приложение
└──────┬──────┘
       │
       ↓
┌─────────────┐
│ GitHub Gist │  ← Облачная база данных (JSON)
└─────────────┘
```

- Приложение читает данные из GitHub Gist
- При создании/изменении ресторана → данные сохраняются в Gist
- Все пользователи видят одни и те же данные

## 🔐 Безопасность

- GitHub Token хранится в localStorage браузера владельца
- Обычные пользователи могут только читать данные
- Token как пароль - держите в секрете!

## 💾 Резервное Копирование

Приложение автоматически создает локальные бэкапы:
- Каждое изменение ресторана логируется
- Можно скачать все бэкапы как JSON
- Вкладка "Backups" в админ-панели

## ❓ Проблемы?

### "Database not configured"
→ Настройте базу данных в админ-панели

### "Invalid GitHub token"  
→ Создайте новый token с правами `gist`

### Рестораны не сохраняются
→ Проверьте, что база данных настроена (зеленая галочка на вкладке Database)

### Несколько администраторов
→ Используйте кнопку "Refresh" для загрузки последних изменений

## 📝 Важные Файлы

- `src/lib/database.ts` - Основная логика работы с БД
- `src/hooks/use-database.ts` - React hook для работы с БД
- `src/components/DatabaseSetup.tsx` - UI настройки БД
- `DATABASE_SETUP_RU.md` - Полная документация

## 🎯 Для Разработчиков

```typescript
// Использование в компонентах
import { useDatabase } from '@/hooks/use-database'

function MyComponent() {
  const { restaurants, isLoading, isConfigured } = useDatabase()
  
  if (!isConfigured) {
    return <div>Database not configured</div>
  }
  
  return <div>{restaurants.map(r => ...)}</div>
}
```

## ⚡ Производительность

- Кэш: 5 секунд (для минимизации API запросов)
- Автоматическая синхронизация при изменениях
- Оптимизированные запросы к GitHub API

---

**Создано для MERIDIEN Yacht Catering**  
Luxury dining delivery service for yacht charters in Dubai
